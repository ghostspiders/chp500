# 核心依赖（M1–M5：数据 + 编制流水线）
akshare
pandas
numpy
pyarrow
pyyaml
python-dotenv

# 后端服务（M6：前后端分离，Python 后端）
fastapi
uvicorn
plotly            # 后端图表库（charts.py 复用；前端用 plotly.js CDN）

# 已弃用：streamlit 单体看板（改为 FastAPI + 静态前端，见 chp500/api、frontend/）
# streamlit

